// Business Map shared-map Firebase configuration.
// Paste the web app config from Firebase Console here to enable cloud sharing.
// The public Firebase web config is safe to ship in client-side code; protect data with Firebase Security Rules.
window.BUSINESS_MAP_FIREBASE_CONFIG = window.BUSINESS_MAP_FIREBASE_CONFIG || null;
